# CLAUDE.md

This file provides guidance to Claude Code (and other AI agents, via the `AGENTS.md` symlink) when working with code in this project.

Before launching a Claude Code session from this subfolder, run `source .claude-project.rc && claude` (or whichever launcher alias) — otherwise Claude Code's native memory/session storage attaches to the monorepo root instead of this subfolder, since `.claude` here is a symlink.

Fill in project-specific guidance here: architecture notes, commands, conventions.
