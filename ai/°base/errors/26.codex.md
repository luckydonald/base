# UI — Codex

## Options for `/plan`:
```text
 Implement this plan?

› 1. Yes, implement this plan          Switch to Default and start coding.
  2. Yes, clear context and implement  Fresh thread. Context: 37% used.
  3. No, stay in Plan mode             Continue planning with the model.

  Press enter to confirm or esc to go back
```

The menu has no free-text acceptance note or denial-reason path. “No, stay in Plan mode” returns to the
ordinary prompt box; its observed hook sequence was `Stop`, then an ordinary `UserPromptSubmit`, with no
`exit_plan_mode` / `ExitPlanMode` event. The two acceptance paths still need independent payload captures.

“Yes, clear context and implement” is a non-text modifier. It is outside this capture scope; never infer a
`context cleared` tag merely from a fresh successor session.

Ordinary “Yes, implement this plan” was exercised: the transcript has the plan item followed by task
completion, while hooks emit only `Stop`; neither the transcript nor hook payloads contain
`exit_plan_mode` / `ExitPlanMode`, a note, or an acceptance field. Because neither acceptance choice has a
text-input path outside normal prompt logging, the clear-context variant is outside this capture scope.

## Options for commands
```text
Would you like to run the following command?

  Environment: local

  Reason: May I read the first line of /etc/os-release to capture the Codex permission menu? This is read-only and makes no repository changes.

  $ sed -n '1p' /etc/os-release

› 1. Yes, proceed (y)
  2. Yes, and don't ask again for commands that start with `sed -n 1p /etc/os-release` (p)
  3. No, and tell Codex what to do differently (esc)

  Press enter to confirm or esc to cancel
```

This menu has no inline text field. The third option is labelled “No, and tell Codex what to do differently”,
but its resulting follow-up path is still untested; do not assume it carries a reason until its event stream is
captured.

#### No, and tell Codex what to do differently
```text
■ Conversation interrupted - tell the model what to do differently. Something went wrong? Hit `/feedback` to report the issue.
```

Option 2 (“don't ask again”) was also exercised. None of the three command choices accepts text outside the
ordinary prompt flow: they proceed, persist an allowlist choice, or interrupt and return to the normal prompt.
There is therefore no Codex command-decision text to record separately.
