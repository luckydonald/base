Design a detailed implementation plan (I will review/trim it into a final plan doc; do not write any code yourself, this is planning only) for a change to the AI-memory system in /home/user/git/luckydonald/base.

## Background (already investigated, don't re-derive)

The repo (a `base` template repo consumed by other projects) has an AI-memory system with three independently-duplicated helper functions that each compute a SINGLE target memory directory for a repo:
- `_memory_dirs(subproject)` in `scripts/°base/ai/hooks/record-memory/hook.py:58-61`
- `_memory_dirs(subproject)` in `scripts/°base/ai/memory/delete.py:23-26`
- `project_memory_dir(root)` in `scripts/°base/ai/hooks/record-codex-memory/hook.py:61-66`

All three do the same thing: `if _is_inside_base_repo(subproject): return subproject/"ai/°base/memory" else: return subproject/"ai/memory"`. `_is_inside_base_repo` lives in `scripts/°base/ai/hooks/°commit_style_lib/__init__.py:34-45` (re-exported via `scripts/°base/ai/hooks/_lib.py:29`) and is true only when the project dir is named `base` and `origin` points at `luckydonald/base`.

- `record-memory/hook.py` mirrors Claude's own per-project memory dir (`$CLAUDE_CONFIG_DIR/projects/<encoded>/memory/*.md`, a SINGLE FLAT directory with no base/root split) into the single repo target dir via hardlinks (`°memory_lib.link_file`), auto-committing. `_sync_all()` (hook.py:142-175) is the SessionStart full-sync: for each file in Claude's source dir, if missing from the repo target dir AND not marked-deleted via a `Deleted Memory: <name>` commit-message marker, it recreates (hardlinks) it into the target dir — this is the resurrection mechanism that will misfire once a second valid target dir exists.
- `record-codex-memory/hook.py` mirrors the SAME repo target dir bidirectionally with a separate Codex-side `resources/<project-key>/` folder, tracked via a `.codex-sync.json` (`sources`/`ignored` maps keyed by a device+path identity string, each entry has a `target` filename and content `hash`). `synchronize_shared_memory()` (hook.py:216-237) does the reverse (resource→project) sync: for each `*.md` in the resource dir not already present in the project's target dir, it hardlinks it INTO the target dir — same resurrection risk. `import_native_note()` (hook.py:200ish, writes `sources[identity]`) and `delete_scoped_memory()` (hook.py:375-415, unlinks by matching `entry.get("target") == name`) are the other two mutators of `.codex-sync.json`.
- `scripts/°base/ai/memory/delete.py` deletes one memory: computes the single dst_dir via its own `_memory_dirs`, requires the file to be `is_tracked` there, calls `°memory_lib.delete_memory(name, src_dir=..., dst_dir=..., dst_dir_rel=...)` (commits with a `Deleted Memory: <name>` marker line — required by the pre-commit hook `scripts/°base/git/hooks/commit/require_memory_delete_marker.py`, which ALREADY scans both `ai/memory/` and `ai/°base/memory/` prefixes generically via its `MEMORY_DIRS = ("ai/memory/", "ai/°base/memory/")` constant — no change needed there), then separately calls into `record-codex-memory/hook.py`'s `delete_scoped_memory` to remove the Codex-side mirror.
- `°memory_lib/__init__.py` re-exports `delete_memory, is_tracked, link_file, same_inode, unlink_path` from `delete.py`/`links.py` in that same package — pure filesystem primitives, no base/root routing logic currently lives here.
- There's a known, already-documented bug (`ai/°base/memory/project_codex_memory_orphan_resource_bug.md`): if a resource-side file has no matching `sources` metadata entry, `delete_scoped_memory` can't find it to remove, so it silently resurrects on the next sync. The NEW problem I'm asking you to design a fix for is a close cousin of this, triggered by a legitimate manual `git mv` between two now-valid target directories rather than a metadata gap.

## The new requirement (confirmed with the user via clarifying questions)

The user manually did a "promote" of one memory (`repo_commit_hooks.md`) from `ai/°base/memory/` to a new root-level `ai/memory/` (see commit `726ab6a0a06c72309af47ac4a450fb974901ae88` in this repo's history for their own mockup of the target shape) — because that memory (a commit-hook/settings convention) is relevant to subprojects too, not just base-repo development. `ai/memory/` is exactly the directory name/convention consuming (non-base) repos already use for their own memory, so promoting something there makes it "available to subprojects" via base's normal template-propagation mechanism. `ai/°base/memory/` remains for memories specifically about developing/maintaining the base tooling itself.

That commit's shape (treat this as the target design for the index-file behavior, already implemented by hand once):
- The real file moves: `git mv ai/°base/memory/repo_commit_hooks.md ai/memory/repo_commit_hooks.md`.
- `ai/memory/MEMORY.md` (a NEW file, root-level index, did not exist before) gains a normal index entry for it (same description text).
- `ai/°base/memory/MEMORY.md`'s existing entry for it is NOT deleted — it's rewritten to a relative-link pointer: `- [Repo commit hooks](../../memory/repo_commit_hooks.md) — <same description>`. So base's own index stays a superset view (own + promoted), while the root index only lists promoted/shared ones (which is what subprojects actually inherit).

Confirmed decisions from the user (do not revisit these, design around them):
1. **Default write target stays `ai/°base/memory/`** for all new/incoming memories in the base repo (both Claude's `record-memory` hook and Codex's auto-import in `record-codex-memory` on `PostToolUse`) — i.e. do NOT change which directory new memories default into. The existing `_is_inside_base_repo` check already used by the three duplicated helpers is the right primitive to keep using for "which dir is primary."
2. **Add a promote/demote script** (one script, e.g. `scripts/°base/ai/memory/promote.py <name> [--demote]`, or your recommended shape) that does the git-mv + both-MEMORY.md-update dance described above, in both directions (promote: °base→root; demote: root→°base, reversing the stub-link placement).
3. **Keep the two `.codex-sync.json` files fully independent** (one scoped to `ai/°base/memory/`, one scoped to `ai/memory/`, each only describing its own directory's contents) — do NOT merge them into one canonical file. The fix for the resurrection bug must work by checking "does a file with this name already exist in the OTHER known directory" as a targeted guard, not by restructuring the metadata schema.

There is CURRENTLY a live, real instance of this exact bug sitting in the repo's working tree right now: `ai/°base/memory/repo_commit_hooks.md` is a resurrected duplicate of `ai/memory/repo_commit_hooks.md` (byte-identical), and `ai/°base/memory/MEMORY.md` has both the correct relative-link stub entry AND a duplicate second entry with a `TODO: summarize this file.` placeholder (from the resurrection). This needs cleaning up as part of landing the fix (not a hypothetical to design around — an actual mess to fix, likely by hand/one-off, separate from the code fix itself).

## What I need from you

A concrete implementation plan covering:
1. **Where to centralize the directory-resolution logic** so it isn't triplicated a 4th way. Decide: does this become a shared `memory_dirs(subproject) -> list[Path]` (ordered, primary first) in `scripts/°base/ai/hooks/_lib.py` (where `_is_inside_base_repo`, `_encoded_project_dir`, `_subproject_root` already live) or in `°memory_lib`? Give exact signatures and call-site diffs for all three current call sites (`record-memory/hook.py`, `record-codex-memory/hook.py`, `delete.py`).
2. **The resurrection-prevention fix**, precisely, for both:
   - `record-memory/hook.py`'s `_sync_all()` (hook.py:142-175) — before hardlinking a Claude-source file into the primary target dir because it's "missing" there, it must check the secondary dir (when one exists) and skip recreating (leave it alone / no-op) if the file already lives there instead. Consider: what happens to the Claude-side source-to-repo hardlink relationship when the authoritative copy lives in the secondary dir — does `_sync_all` need to hardlink src↔secondary-dir instead of src↔primary-dir in that case? Work out the correct hardlink target selection.
   - `record-codex-memory/hook.py`'s `synchronize_shared_memory()` (hook.py:216-237) — same guard, applied to the resource→project reverse-sync direction, respecting that `.codex-sync.json` stays per-directory (so also work out: does the file's `sources`/`ignored` entry, if any, need to move between the two `.codex-sync.json` files when the underlying file moves, and if so who's responsible for that — the sync hook noticing a moved file, or only the new promote/demote script performing an explicit move?).
3. **`delete.py` updated to find/delete a memory regardless of which of the two directories currently holds it** (today it only looks in the single dir its own `_memory_dirs` computes) — needed so a promoted memory can still be deleted normally.
4. **The new `promote.py` (with `--demote`) script**: exact CLI shape, step-by-step algorithm (git mv, MEMORY.md edits in both files per the stub-link convention shown above, relocating any matching `.codex-sync.json` entry from the source dir's file to the destination dir's file, preserving its `hash`/identity, updating its `target`), and the commit it creates. Work out whether the commit needs a `Deleted Memory: <name>` marker line to satisfy `require_memory_delete_marker.py` (it treats a `git diff --cached --diff-filter=D` hit under either memory dir as a deletion requiring the marker — check empirically/by reasoning whether a `git mv`-staged rename shows up as `D`+`A` or as `R` under default git-diff rename-detection settings for this repo, since that determines whether the marker is even required) and if so include it (self-authored, deliberate, not a real "loss").
5. **Test plan**: which existing tests in `scripts/°base/tests/test_ai_hooks_base_routing.py` and `scripts/°base/tests/test_memory_delete.py` need updating (if any) vs. which stay valid as-is (e.g. `test_memory_in_base_repo_routes_and_prefixes` — does its `assertFalse(ai/memory exists)` still hold given nothing in that test promotes anything?), and what NEW tests to add for: resurrection-prevention in both hooks, `delete.py` deleting a promoted memory, and a `promote.py`/`demote.py` round trip (file moved, both MEMORY.md's correct, codex-sync entries relocated when present, commit passes the real pre-commit hooks).
6. **The one-off cleanup** of the currently-live duplicate (`ai/°base/memory/repo_commit_hooks.md` + its duplicate MEMORY.md line) — just note it as a cleanup step, e.g. via the new `promote.py`... no wait, it can't use promote.py since the file's already IN both places; specify exactly what to remove/fix by hand (which file, which MEMORY.md lines) to reach the clean target state (file only in `ai/memory/`, single correct stub line in `ai/°base/memory/MEMORY.md`).

Read the actual current file contents before proposing diffs (don't guess line numbers) — at minimum `scripts/°base/ai/hooks/record-memory/hook.py`, `scripts/°base/ai/hooks/record-codex-memory/hook.py`, `scripts/°base/ai/memory/delete.py`, `scripts/°base/ai/hooks/°memory_lib/__init__.py` + `links.py`, `scripts/°base/ai/hooks/_lib.py`, `scripts/°base/ai/memory/import-codex.py`, `scripts/°base/git/hooks/commit/require_memory_delete_marker.py`, and the relevant test file sections. Also read the two current `.codex-sync.json` files and both current `MEMORY.md`/`repo_commit_hooks.md` files under `ai/°base/memory/` and `ai/memory/` to see the exact live mess to clean up.

Report back a structured plan (file-by-file diffs described precisely with function names/line ranges, not full code) — I will fold your findings into the user-facing plan doc myself, so prioritize completeness and precision over prose.