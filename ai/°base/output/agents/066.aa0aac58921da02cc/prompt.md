In the repo /home/user/git/luckydonald/base, search for any existing handling of Claude Code's project-directory/path-encoding for its native memory or session storage under `~/.claude/projects/<encoded-path>/`. Specifically:

1. Search `.claude/settings.json` (at repo root) for any field related to project directory, project root, memory path overrides, or similar (grep for "project", "memory", "CLAUDE_PROJECT_DIR" case-insensitively).
2. Search the whole repo (excluding `.git`, `node_modules`, `__pycache__`) for any reference to `CLAUDE_PROJECT_DIR` — where is it read, where (if anywhere) is it *set/exported* rather than just read? E.g. grep `CLAUDE_PROJECT_DIR` across `scripts/°base/` and any shell rc/init scripts, `.envrc`, wrapper scripts for launching `claude`.
3. Search for any existing awareness in this codebase (comments, docs, plan files under `ai/°base/plans/`, memory files under `ai/°base/memory/` or `ai/memory/`) of the specific issue: symlinked `.claude` causing Claude Code's own project-identity resolution (for its native memory feature, not this repo's custom `record-memory` hook) to collapse to the symlink target's real path instead of the literal subfolder path. Grep for terms like "realpath", "project identity", "encoded project", "native memory", "symlink" combined with "claude" or "memory".
4. Check whether there's any wrapper/launcher script in this repo that invokes the `claude` binary itself (as opposed to hooks that run *within* a claude session) — e.g. anything under `scripts/` that does `exec claude` or similar — which could be a place to export an env var before launch.

Report back with file:line citations for anything found, and explicitly say "nothing found" for any of the 4 items where a thorough search turned up nothing. This is read-only investigation, no edits.