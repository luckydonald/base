I'm investigating a bug in a monorepo tooling setup (not asking you to write code — just need product-behavior facts).

Setup: a repo has a root `.claude/` directory (settings.json, hooks, etc). A helper script creates subfolders like `my_subproject/` inside the repo and symlinks `my_subproject/.claude -> ../.claude` (a directory symlink) so that launching `claude` from inside `my_subproject/` picks up the same shared settings/hooks/permissions as the repo root, without duplicating config.

Observed bug: when a Claude Code session is launched with cwd = `my_subproject/` (a fresh process, not an old stale session), the session's own `cwd` and `transcript_path` correctly reflect the subfolder path (e.g. `.../my_subproject/<session-id>.jsonl`). However, whichever mechanism Claude Code uses to compute the on-disk path for its own automatic/native memory feature (the directory under `~/.claude/projects/<encoded-project-path>/memory/` that gets referenced in the "auto memory" system-prompt block) does NOT use `my_subproject`'s literal path — it resolves to the encoded path of the *symlink target's real path* instead, i.e. the parent repo root. This means memories written by the native memory system during a session launched inside `my_subproject/` land in the parent repo's project bucket, not a subfolder-specific one — because `.claude` is a symlink and something in Claude Code's project-identity/path-encoding logic appears to call something like `realpath()`/`os.path.realpath` on the `.claude` directory (or on cwd while searching upward for the nearest `.claude`) rather than using the literal, non-symlink-resolved cwd.

Questions:
1. Is this a known/documented behavior of Claude Code's project-path encoding for `~/.claude/projects/<encoded>/`? Does it use `os.getcwd()`, `os.path.realpath(cwd)`, or something else (e.g. walking up to find the nearest `.claude` dir and using ITS realpath) to compute the encoded project directory used for memory/session storage?
2. Is there any documented environment variable, CLI flag, or `.claude/settings.json` field that lets you override or pin the project directory / project identity Claude Code uses for this path encoding, independent of where `.claude` physically resolves to via symlinks? (e.g. does `CLAUDE_PROJECT_DIR` — which is documented as available inside hooks — also affect this native project-identity/path-encoding logic, or is it only exposed to hook subprocesses without influencing this?)
3. Is symlinking `.claude` as a whole directory (rather than symlinking individual files inside a real `.claude` directory) a known anti-pattern for monorepo/subproject setups, for exactly this reason?

Please answer from documentation/known behavior, and say clearly if something is not documented/not knowable rather than guessing. Keep the answer focused and under 400 words.