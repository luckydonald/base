I need permission to run the script. Claude Code is in don't-ask mode, so I can't execute `scripts/download_ref.py --no-ide` without your explicit approval.

Could you grant permission to run Bash commands, or would you prefer to run the script yourself from the terminal?