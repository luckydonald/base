In the repo /home/user/git/luckydonald/base, investigate the `ai/°base/errors/` directory (it contains numbered files like `25.txt`). Look for:
1. Any documentation (AGENTS.md, README, skill, hook, or script) describing what this directory is for and what should happen to an error file once it's been diagnosed/fixed (e.g., should it be deleted, moved, marked resolved?).
2. Any script that writes into `ai/°base/errors/` (to understand what triggers an error file's creation).
3. List the other files currently in `ai/°base/errors/` for context (just names).

Report factually, under 250 words.